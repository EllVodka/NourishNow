{% extends 'base.html.twig' %}

{% block title %}Hello LivreurController!{% endblock %}

{% block body %}
<link rel="stylesheet" href="/styles/livreur.css">

<div class="parent-card-livreur">
    {% for c in commandes_prete %}
    <div class="card-livreur">
        <p>{{ c.detailCommandes[0].fkplat.fkrestaurant.lieu }}, {{ c.detailCommandes[0].fkplat.fkrestaurant.fkville.libelle }}</p>
        <p>{{c.fkclient.Adresse}}, {{c.fkclient.fkville.libelle}}</p>
        <p>Status : {{ c.fkstatus.libelle}}</p>
    
        <p>Heure livraison : {{ c.date | date("j/m H:i", "Europe/Paris") }}h</p>
    
        <a class="btn-card-livreur" href="{{path( 'app_view_commande_take',{'id' : c.id} ) }}"><i class="fa-solid fa-check fa-2xl"></i></a>

    </div>


    {% else %}
    <h1></h1>
    <h1>Il n'y a pas de commande prête pour le moment</h1>
    <h1></h1>
    {% endfor %}
</div>
</div>
{% endblock %}